- не закрывается *.shader во время игры, но ведь он должен закрывать ся сразу после прочтения
- юниты при малом количестве кадров могут бесконечно вертеться на месте
- вуду 3000
- при трассировке луча в БСП не должны учитываться односторонние треугольники, повернутые задом
- при трассировке луча в объектах/людях нужно пропучкать шейдеры с определенными параметрами
- необходимо переделать код скелетной анимации, чтобы можно было грузить все из пака
- при малом количестве fps человек/техника может долго крутиться
- при запуске двух ракет иногда становится прозрачным часть интерфейса (\\Flif\Executable\gluc_screenshot)
- save, load - при загрузке сейва не грузятся текстуры
- какая-то беда с мультитекстурированием - мигают текстуры
-
